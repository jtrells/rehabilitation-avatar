enum ExerciseStatus {
    Preparing = 0,
    Running = 1,
    Finished = 2,
    Pause = 3,
    Calibration = 4
}

enum Perspective {
    First = 1,
    Third = 3
};

enum TrainingMode {
    Normal = 1,
    Distorted = 2,
    Trajectory = 3,
    DistortedAndTrajectory = 4
}

enum TrainingType {
    Tutorial = 1,
    Random = 2,
    ProgressiveDistance = 3,
    Custom = 4
}

enum CalibrationAxis {
    X = 1,
    Y = 2,
    Z = 3
}